<!DOCTYPE html>
<html lang="en">
    
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kirpputori - Login</title>
    <link rel="stylesheet" href="/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
     <div class="custom-container">
        <h1 class="text-center m-4 text-success">Kirpputori</h1>
        <div class="jumbotron">
            <h3 class="text-center text-primary">Kirjaudu sisään!</h3>
            <br>
            {{#if success}}
            <div class="alert alert-success alert-dismissible fade show">
                <strong>{{success}}</strong>
                <button class="close" data-dismiss="alert"><span>&times;</span></button>
            </div>
            {{/if}}
            {{#if error}}
            <div class="alert alert-danger alert-dismissible fade show">
                <strong>{{error}}</strong>
                <button class="close" data-dismiss="alert"><span>&times;</span></button>
            </div>
            {{/if}}

            <form action="" id="login" method="POST">
                <div class="form-group">
                    <label for="email">Sähköposti <span class="text-danger" >*</span></label>
                    <input type="email" class="form-control" name="username" id="email" placeholder="Syötä rekisteröity sähköposti">
                </div>
                <div class="form-group">
                    <label for="password">Salasana <span class="text-danger">*</span></label>
                    <input type="password" class="form-control" name="password" id="password" placeholder="Syötä salasanasi">
                </div>
                <div class="form-group">
                    <input type="submit" value="Kirjaudu" class="btn btn-info btn-block">
                </div>
                <div class="form-group">
                    <div class="row">
                        <div class="col-md-8">
                            Oletko uusi? Rekisteröidy <a href="/register">tästä!</a>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
